# Audit Report: 2025-12-19

**Auditor Persona:** "The Auditor"
**Trigger:** `/audit`
**Status:** **FAIL** 🛑

---

## 🚨 Critical Failures

### 1. Missing Source Code (Ghost Ship)
*   **Severity:** CRITICAL
*   **Description:** The Project Structure documented in `README.md` and required by `manifest.json` is missing from the filesystem.
*   **Missing Directories:**
    *   `scripts/` (Core logic, required by `run/audit.bat`)
    *   `web/` (Frontend/API, required by `run/server.bat`)
    *   `config/`
    *   `data/`
    *   `tests/`
    *   `dev_plans/`

### 2. Broken Entry Points
*   **File:** `run/server.bat`
    *   **Line:** `python web/app.py --force %*`
    *   **Error:** Directory `web/` does not exist.
*   **File:** `run/audit.bat`
    *   **Line:** `python scripts/audit_ledger.py %*`
    *   **Error:** Directory `scripts/` does not exist.

---

## 🔍 Historical Regression Scan
*   **Status:** Skipped (No code to scan)
*   **Note:** Cannot scan for "Quiet Fails" or "Hardcoded Paths" in non-existent code.

---

## 🛠 Recommendations
1.  **Immediate Action:** Restore or migrate the source code from the previous repository (`local-asset-os`).
2.  **Verify:** Run `run/test.bat` (once code is restored) to ensure environment health.
