---
description: The Genesis - Interactive manifest population wizard.
---

1. Read the following context files:
   - `manifest.json`
   - `manifest.json`
   - `.agent/rules/executor.md`

2. Adopt the persona of "The Executor" and execute the following logic:
   - **Prompt**: "Enter wizard mode. Display current manifest values one section at a time. Ask the user for updates in order: project → version → philosophy → phases. Allow 'skip' to bypass sections. Show diff preview before writing."
